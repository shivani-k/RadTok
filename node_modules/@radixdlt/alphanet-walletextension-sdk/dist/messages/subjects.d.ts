import { Subject } from 'rxjs';
import { IncomingMessageType, MessageDispatch, OutgoingMessageType } from './_types';
import { SdkError } from '../errors';
export declare type SubjectsType = ReturnType<typeof Subjects>;
export declare const Subjects: () => {
    outgoingMessageSubject: Subject<OutgoingMessageType>;
    incomingMessageSubject: Subject<IncomingMessageType>;
    responseSubject: Subject<SdkError | {
        method: "request";
        requestId: string;
        payload: import("../methods").AccountAddressResponse[];
    } | {
        method: "sendTransaction";
        requestId: string;
        payload: {
            transactionHash: string;
        };
    }>;
    messageLifeCycleEventSubject: Subject<{
        eventType: "receivedByExtension";
        requestId: string;
    }>;
    dispatchEventSubject: Subject<MessageDispatch>;
};
