import { OutgoingMessage, OutgoingMessageType } from './_types';
import { Result } from 'neverthrow';
declare type CreateMessageInput = Omit<OutgoingMessage['request'], 'requestId'> | Omit<OutgoingMessage['sendTransaction'], 'requestId'>;
export declare const createMessage: (input: CreateMessageInput, requestId?: string) => Result<OutgoingMessageType, never>;
export {};
