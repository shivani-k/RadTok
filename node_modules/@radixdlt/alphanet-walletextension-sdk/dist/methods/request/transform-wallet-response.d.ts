import { SdkError } from '../../errors';
import { Result } from 'neverthrow';
import { WalletResponses } from '../_types';
export declare const transformWalletResponse: (result: Result<WalletResponses['request'], SdkError>) => Result<Partial<{
    accountAddresses: {
        label: string;
        address: string;
    }[];
}>, SdkError>;
