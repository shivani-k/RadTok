import { SdkError } from '../errors';
import { IncomingMessageType, OutgoingMessageType } from '../messages';
import { IncomingMessage as AlphaIncomingMessage, OutgoingMessage as AlphaOutgoingMessage } from './_types';
import { OutgoingMessageEvent } from '../messages/events/_types';
export declare const bridge: {
    transformIncomingMessage: (message: AlphaIncomingMessage | IncomingMessageType) => IncomingMessageType | SdkError;
    transformOutgoingMessage: (input: {
        event: OutgoingMessageEvent;
        payload: OutgoingMessageType;
    }) => {
        payload: AlphaOutgoingMessage;
        event: OutgoingMessageEvent;
    };
};
